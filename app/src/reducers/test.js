import createReducer from '../lib/createReducer'
import * as types from '../actions/types'

/**
    export const stateToProps = createReducer({ initialState },{
        [types.TEST_TYPE](state,action){
            ...state
            return state
        }
    })
*/
